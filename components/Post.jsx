import Link from "next/link";

const Post = ({ type, post, setPost, submitting, handleSubmit }) => {
  return <div>
    <section>
      <h1>Post</h1>
    </section>
  </div>;
};

export default Form;
